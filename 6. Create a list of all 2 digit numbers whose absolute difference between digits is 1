def diff(i):
    while i>0:
        r=i%10
        if r+1==(i//10)%10 or r-1==(i//10)%10:
            return True
        else:
            return False
nums=[i for i in range(10,100) if diff(i)==True]
print(nums)
